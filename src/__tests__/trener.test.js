//const aws = require('../../amplify/backend/function/trener/src/node_modules/aws-sdk')
//let { handler } = require('../../amplify/backend/function/trener/src/index')

describe('trener should', () => {
  test.todo('throw an error if no user was specified')
  test.todo('work without a cognito identity')
  test.todo('throw an error if user is not the same as the identity')
  test.todo('throw an error if this user has no card')
  test.todo('get the appropriate card, and send it to the user')
  test.todo('update the showed statistics of the card it chose')
})