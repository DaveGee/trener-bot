type Card @model @auth(rules: [{allow: owner}]) {
  id: ID!
  question: String!
  answer: String
  createdAt: AWSDateTime
  owner: String
  stats: Stats!
}

type Stats {
  showed: Int!
  correct: Int!
  wrong: Int!
}